import React from 'react';
import classes from './Contact.module.css';

const contact = (props) => (
  <div className={classes.Test}>This is going to be the Contact section.</div>
)

export default contact;