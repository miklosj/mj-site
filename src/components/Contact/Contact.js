import React from 'react';
import styles from './Contact.module.css';

const contact = (props) => (
  <div className={styles.Test}>This is going to be the Contact section.</div>
)

export default contact;