import React from 'react';
import classes from './About.module.css';

const about = (props) => (
  <div className={classes.Test}>This is going to be the About Me section.</div>
)

export default about;